[build]
  base = "app"
  command = "npm install && npx vite build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://alona-nonforested-remedios.ngrok-free.dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
